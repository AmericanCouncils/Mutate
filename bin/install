#!/usr/bin/env php
<?php

//TODO: this script should globally "install" mutate on the system by doing the following:

//run compile command, move to default global location (/usr/bin) (or accept override path in cli)
//copy mutate.conf.php to global location (/etc/mutate.conf.php) (or accept override path in cli)
//print out status